# Essay workspace local verification — September 23, 2026

No deployment or live API writes. Existing preview server at 127.0.0.1:4173 was reused. Only the local synthetic reviewer `Essay local QA` was modified; no production reviewer records were loaded or changed.

- 13 automated tests passed: existing access/revision/autosave behavior, separate rankings, essay payload round-trip, local name isolation/sign-out persistence, character and word boundaries.
- All 12 encrypted seed drafts meet their configured limits. Stable IDs are prefixed `essay-`; ABS payload and IDs remain unchanged.
- Chrome: opened essays, opened a local review, edited an essay, saw 1701/1700 overflow, restored the draft, added/edited/unsent/restored a comment, reloaded, reopened and verified the comment and draft persisted.
- Chrome: NOSM 51/50 words shows overflow in the draft and suggested-wording composer. Restored the draft and cleared the unsent test suggestion.
- Chrome: checked original response disclosure, prompt summaries, source link, all seven TMU/five NOSM response choices, and the Ottawa exclusion panel.
- Responsive check: a narrow phone viewport had equal document scroll/client widths (416/416 reported); screenshot showed legible editor, counters and controls without horizontal overflow. Viewport override reset afterward.
- Final preview signed out of synthetic reviewer, opened Essays & circumstances, and left ready for the user. The test comment was unsent; synthetic draft edits were restored.
- `public/sketch.enc.json` unchanged from HEAD. Plaintext drafts and keys are excluded from git. `public/config.json`, backend code and deployment files unchanged.

Unresolved content is deliberately flagged: exact affected academic terms, early accommodation dates, some regional/community details and final OMSAS ABS numbers. No submission-ready certification is implied.

## Later research correction, same day

The checks above describe the first implementation, before direct inspection of the NOSM PDF's field metadata. That inspection established additional 250/500-character caps. The earlier word-only fit claim is therefore superseded.

- Updated counters show hard characters and approximate words together. Four original seed drafts are now accurately marked over the PDF cap; all draft and original strings remain identical.
- 14 automated tests pass, including a short-word-count answer that exceeds the PDF character cap and a response exceeding approximate word guidance without violating its hard character cap.
- Chrome read-only check of all five NOSM fields: 292/250 (42 over), 265/250 (15 over), 367/500, 618/500 (118 over), 595/500 (95 over). Each also shows its approximate word guidance.
- Packed the unfinished local review using explicit `--review-drafts`; no deploy or live writes. Reviewer edits/comments were not changed in this follow-up.
